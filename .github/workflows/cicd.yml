name: cicd

on: push
jobs:
  ci:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v3
      - name: Change directory
        run: cd nodejs-server-server-generated/package.json
      - name: Install dependencies
        run: npm install
      - name: Run test
        run: npm test
